# Hostinger Corepack invoque pnpm@12.4.1 (plus de bin/pnpm.cjs) dès qu'il voit un lock pnpm.
# L'install production passe donc par npm (package-lock.json + workspaces).
# En local, pnpm 9 reste utilisable : pnpm-lock.yaml et pnpm-workspace.yaml.
engine-strict=false
legacy-peer-deps=true
