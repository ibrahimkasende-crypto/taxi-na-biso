# Hostinger fournit pnpm 12.x ; le pin packageManager reste la source de vérité.
# Sans cette option, un écart de version mineur bloque l'install (corepack cache).
package-manager-strict=false
engine-strict=false
