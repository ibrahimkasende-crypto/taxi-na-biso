# Ops scripts & runbooks

Placeholders. Real runbooks land alongside Phase 8 (test, deploy, docs).

- `backups/` — manual `supabase db dump` snapshots prior to risky migrations.
- `runbooks/` — incident playbooks (Stripe webhook backlog, Realtime disconnects, etc.).
